{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Web (Next.js)",
      "type": "node",
      "request": "launch",
      "cwd": "${workspaceFolder}/apps/web",
      "runtimeExecutable": "npm",
      "runtimeArgs": ["run", "dev", "--", "--hostname", "0.0.0.0", "--port", "3001"],
      "env": {
        "NODE_OPTIONS": "--inspect"
      },
      "restart": true,
      "console": "integratedTerminal",
      "serverReadyAction": {
        "pattern": "- Local:.+(https?://.+)",
        "uriFormat": "%s",
        "action": "debugWithChrome"
      }
    },
    {
      "name": "API (FastAPI)",
      "type": "debugpy",
      "request": "launch",
      "cwd": "${workspaceFolder}/services/api",
      "python": "${workspaceFolder}/services/api/.venv/bin/python",
      "module": "uvicorn",
      "args": ["app.main:app", "--reload", "--host", "0.0.0.0", "--port", "5001"],
      "env": {
        "MC_API_ENV": "dev"
      },
      "console": "integratedTerminal"
    }
  ],
  "compounds": [
    {
      "name": "Full Stack (Web + API)",
      "configurations": ["Web (Next.js)", "API (FastAPI)"]
    }
  ]
}
